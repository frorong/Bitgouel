import styled from "@emotion/styled";

export const Wrapper = styled.div`
  display: flex;
  justify-content: space-between;

  width: 100vw;
  height: 33.75rem;

  position: relative;
  overflow: hidden;

  img {
    object-fit: cover;
  }
`;
